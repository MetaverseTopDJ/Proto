syntax = "proto3";

package member;

option go_package = "./pb;pb";

/**************** 数据结构 ****************/

// Card 会员卡
message Card {
  uint64 id            = 1;
  uint64 activity_id   = 2;
  string name          = 3;
  uint64 type          = 4;
  string icon          = 5;
  string cover         = 6;
  string desription    = 7;
  uint64 total         = 8;
  uint64 remain        = 9;
  uint64 price         = 10;
  uint64 decimal       = 11;
  uint64 status        = 12;
  bool   checked       = 13;
  string created_at    = 14;
  uint64 created_stamp = 15;
  string updated_at    = 16;
  uint64 updated_stamp = 17;
}

/**************** 请求结构 ****************/

// CreateCardPost 创建卡片请求
message CreateCardPost {
  uint64 activity_id = 2;
  string name        = 3;
  uint64 type        = 4;
  string icon        = 5;
  string cover       = 6;
  string desription  = 7;
  uint64 total       = 8;
  uint64 price       = 9;
  uint64 decimal     = 10;
}

// UpdateCardPost 更新卡片请求
message UpdateCardPost {
  uint64 id          = 1;
  uint64 activity_id = 2;
  string name        = 3;
  uint64 type        = 4;
  string icon        = 5;
  string cover       = 6;
  string desription  = 7;
  uint64 total       = 8;
  uint64 remain      = 9;
  uint64 price       = 10;
  uint64 decimal     = 11;
}

/**************** 返回结构 ****************/

// CardResponse 返回会员卡
message CardResponse {
  string message = 1;
  Card   data    = 2;
}

// CardsResponse 返回卡片列表
message CardsResponse {
  string   message     = 1;
  repeated Card   data = 2;
}

// CardPaginationResponse 卡片分页
message CardPaginationResponse {
  message Cards {
    repeated Card list = 1;
    int64    total     = 2;
  }
  string message = 1;
  Cards  data    = 2;
}